name: Check website scheduled run

on:
  #TODO Remove comments to start scheduler
  # schedule:
  #   # Run every hour at minute 0
  #   - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: playwright install chromium

      - name: Run website check
        env:
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_FROM_NUMBER: ${{ secrets.TWILIO_FROM_NUMBER }}
          TWILIO_TO_NUMBER: ${{ secrets.TWILIO_TO_NUMBER_CY }}
        run: |
          python check-website.py \
            "${{ secrets.WEBSITE_URL }}" \
            "${{ secrets.WEBSITE_HTML_SELECTOR }}" \
            "${{ secrets.WEBSITE_EXPECTED_TEXT }}"
